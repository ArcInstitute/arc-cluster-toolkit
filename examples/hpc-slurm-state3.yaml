# Copyright 2022 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---

blueprint_name: hpc-slurm-state3

vars:
  project_id: arc-state3
  deployment_name: hpc-slurm-state3
  region: us-east1
  zone: us-east1-b
  enable_cleanup_compute: true
  slurm_image:
    family: arc-state3-slurm-docker
    project: arc-state3
  disk_size: 50

deployment_groups:
- group: primary
  modules:
  - id: network
    source: modules/network/vpc

  - id: homefs
    source: modules/file-system/filestore
    use: [network]
    settings:
      local_mount: /home
      size_gb: 5120

  - id: datafs
    source: modules/file-system/filestore
    use: [network]
    settings:
      local_mount: /data
      size_gb: 10240

  - id: state3-data-curation-bucket
    source: modules/file-system/pre-existing-network-storage
    settings:
      remote_mount: state3-data-curation
      fs_type: gcsfuse
      local_mount: /mnt/gcs/state3-data-curation
      mount_options: defaults,_netdev,implicit_dirs,allow_other,dir_mode=0777,file_mode=766

  - id: ssh_script
    source: modules/scripts/startup-script
    settings:
      configure_ssh_host_patterns: ['hpcslurmst-*']

  # Login-node-only startup: SSH host config + OS hardening. Kept separate from
  # ssh_script (which is shared with the debug nodeset) so the per-user resource
  # caps below apply ONLY to the login node, never to compute nodes running jobs.
  - id: login_setup
    source: modules/scripts/startup-script
    settings:
      configure_ssh_host_patterns: ['hpcslurmst-*']
      runners:
      - type: shell
        destination: arc_login_hardening.sh
        content: |
          #!/bin/bash
          # Arc login-node hardening:
          #  (1) Cap per-user memory/CPU so one user cannot OOM or CPU-starve the
          #      shared login node. With per-user-slice limits, the OOM-killer
          #      stays inside the offending user's cgroup, protecting sshd, system
          #      services, and other users. Heavy compute belongs in Slurm jobs.
          #  (2) Persist journald so OS-level root-cause logs survive reboots
          #      (the default is volatile/RAM-only, wiped on every boot).
          # See docs/state3-slurm.md ("Login-node resource limits").
          set -euo pipefail
          mkdir -p /etc/systemd/system/user-.slice.d
          printf '%s\n' \
            '# Arc: per-user resource caps on the login node' \
            '[Slice]' 'MemoryAccounting=yes' 'MemoryMax=8G' \
            'CPUAccounting=yes' 'CPUQuota=200%' \
            > /etc/systemd/system/user-.slice.d/50-arc-limits.conf
          mkdir -p /etc/systemd/journald.conf.d
          printf '%s\n' \
            '[Journal]' 'Storage=persistent' 'SystemMaxUse=1G' \
            > /etc/systemd/journald.conf.d/50-arc-persistent.conf
          mkdir -p /var/log/journal
          systemd-tmpfiles --create --prefix /var/log/journal || true
          systemctl daemon-reload
          systemctl restart systemd-journald || true

  - id: compute_startup_script
    source: modules/scripts/startup-script
    settings:
      configure_ssh_host_patterns: ['hpcslurmst-*']
      local_ssd_filesystem:
        fs_type: ext4
        mountpoint: /scratch
        permissions: "1777"

  - id: debug_nodeset
    source: community/modules/compute/schedmd-slurm-gcp-v6-nodeset
    use: [network, ssh_script]
    settings:
      node_count_dynamic_max: 4
      machine_type: n2-standard-8
      enable_placement: false
      allow_automatic_updates: false
      instance_image: $(vars.slurm_image)
      instance_image_custom: true

  - id: debug_partition
    source: community/modules/compute/schedmd-slurm-gcp-v6-partition
    use:
    - debug_nodeset
    settings:
      partition_name: debug
      exclusive: false
      is_default: true

  - id: compute_nodeset
    source: community/modules/compute/schedmd-slurm-gcp-v6-nodeset
    use: [network, compute_startup_script]
    settings:
      node_count_dynamic_max: 20
      machine_type: c4d-standard-32-lssd
      disk_type: hyperdisk-balanced
      disk_size_gb: 100
      bandwidth_tier: gvnic_enabled
      allow_automatic_updates: false
      instance_image: $(vars.slurm_image)
      instance_image_custom: true
      enable_spot_vm: true
      spot_instance_config:
        termination_action: DELETE

  - id: compute_partition
    source: community/modules/compute/schedmd-slurm-gcp-v6-partition
    use:
    - compute_nodeset
    settings:
      partition_name: compute

  - id: computelarge_nodeset
    source: community/modules/compute/schedmd-slurm-gcp-v6-nodeset
    use: [network, compute_startup_script]
    settings:
      node_count_dynamic_max: 20
      machine_type: c4d-standard-64-lssd
      disk_type: hyperdisk-balanced
      disk_size_gb: 100
      bandwidth_tier: gvnic_enabled
      allow_automatic_updates: false
      instance_image: $(vars.slurm_image)
      instance_image_custom: true
      enable_spot_vm: true
      spot_instance_config:
        termination_action: DELETE

  - id: computelarge_partition
    source: community/modules/compute/schedmd-slurm-gcp-v6-partition
    use:
    - computelarge_nodeset
    settings:
      partition_name: computelarge

  - id: slurm_login
    source: community/modules/scheduler/schedmd-slurm-gcp-v6-login
    use: [network]
    settings:
      machine_type: n2-standard-4
      enable_login_public_ips: true
      instance_image: $(vars.slurm_image)
      instance_image_custom: true

  - id: slurm_controller
    source: community/modules/scheduler/schedmd-slurm-gcp-v6-controller
    use:
    - network
    - debug_partition
    - compute_partition
    - computelarge_partition
    - homefs
    - datafs
    - state3-data-curation-bucket
    - slurm_login
    settings:
      enable_controller_public_ips: true
      login_startup_script: $(login_setup.startup_script)

- group: packer
  modules:
  - id: slurm-docker-image
    source: modules/packer/custom-image
    kind: packer
    use: [network]
    settings:
      source_image_project_id: [schedmd-slurm-public]
      source_image_family: slurm-gcp-6-8-hpc-rocky-linux-8
      disk_size: $(vars.disk_size)
      image_family: $(vars.slurm_image.family)
      machine_type: n2-standard-4
      state_timeout: 15m
      startup_script: |
        #!/bin/bash
        set -e
        dnf config-manager --add-repo https://download.docker.com/linux/rhel/docker-ce.repo
        dnf -y install docker-ce docker-ce-cli containerd.io \
          docker-buildx-plugin docker-compose-plugin
        systemctl enable docker.socket
        mkdir -p /etc/systemd/system/docker.socket.d
        printf '[Socket]\nSocketMode=0666\n' \
          > /etc/systemd/system/docker.socket.d/override.conf
        systemctl daemon-reload
        dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
        dnf -y install gh
